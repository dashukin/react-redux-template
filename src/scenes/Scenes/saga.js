/*
* Scenes sagas
* */

import {put, fork, spawn, takeEvery} from 'redux-saga/effects';

// import watch sagas

const watchScenes = function* () {
	yield [
		// put all watch sagas here
	]
};

export {
	watchScenes
};