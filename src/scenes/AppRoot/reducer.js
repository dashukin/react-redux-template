/*
* AppRoot reducer
* */

const initialState = {
	//
};

const appReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
}

const reducers = {
	appReducer
}

export default reducers;