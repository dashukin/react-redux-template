import React, { PureComponent } from 'react';
import './_home.scss';

class HomePage extends PureComponent {
  render() {
    return (
      <div>
        Home page
      </div>
    );
  }
}

HomePage.propTypes = {
  // define any required
};

export default HomePage;
