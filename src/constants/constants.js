/*
* Constants
* */

import keymirror from 'keymirror';

const constants = keymirror({
	EXAMPLE_1: null
});

const values = {
	EXAMPLE_2: 'example-2'
};

export default Object.assign({}, constants, values);