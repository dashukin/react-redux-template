/**
* {{properCase name}} reducer
*/

import constants from './{{name}}.constants';

const {
{{#each constants}}
    {{constantCase name}},
{{/each}}
} = constants;

const initialState = {};

const {{camelCase name}} = (state = initialState, action) => {
    const { type, payload } = action;

    switch (type) {
        {{#each actions}}
            case {{constantCase type}}:
            return state;

        {{/each}}
        default:
        return state;
    }
};

export default {{camelCase name}};
